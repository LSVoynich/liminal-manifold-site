# Custom 404 page
ErrorDocument 404 /404.html

# Redirect www to non-www (canonical domain)
RewriteEngine On
RewriteCond %{HTTP_HOST} ^www\.liminalmanifold\.com [NC]
RewriteRule ^(.*)$ https://liminalmanifold.com/$1 [R=301,L]

# Redirect old Almost Supernova URLs to Quantum Eve
RewriteRule ^artists/almost-supernova(/.*)?$ /artists/quantum-eve$1 [R=301,L]

# Redirect old Zara Pixel URLs to Quantum Eve
RewriteRule ^artists/zara-pixel(/.*)?$ /artists/quantum-eve$1 [R=301,L]

# ─── Security Headers ───
<IfModule mod_headers.c>
  # Prevent clickjacking
  Header always set X-Frame-Options "SAMEORIGIN"

  # Prevent MIME-type sniffing
  Header always set X-Content-Type-Options "nosniff"

  # XSS protection (legacy browsers)
  Header always set X-XSS-Protection "1; mode=block"

  # Referrer policy
  Header always set Referrer-Policy "strict-origin-when-cross-origin"

  # HSTS - enforce HTTPS (1 year)
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"

  # Permissions policy - restrict browser features
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()"
</IfModule>

# ─── Compression ───
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE text/javascript
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/json
  AddOutputFilterByType DEFLATE application/xml
  AddOutputFilterByType DEFLATE text/xml
  AddOutputFilterByType DEFLATE image/svg+xml
  AddOutputFilterByType DEFLATE application/rss+xml
</IfModule>

# ─── Browser Caching ───
<IfModule mod_expires.c>
  ExpiresActive On

  # HTML - short cache (content changes)
  ExpiresByType text/html "access plus 1 hour"

  # CSS & JS - long cache (fingerprinted by Astro)
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType text/javascript "access plus 1 year"

  # Images - long cache
  ExpiresByType image/webp "access plus 6 months"
  ExpiresByType image/png "access plus 6 months"
  ExpiresByType image/jpeg "access plus 6 months"
  ExpiresByType image/svg+xml "access plus 6 months"
  ExpiresByType image/x-icon "access plus 1 year"

  # Fonts - long cache
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType application/font-woff2 "access plus 1 year"

  # XML/RSS - short cache
  ExpiresByType application/xml "access plus 1 hour"
  ExpiresByType application/rss+xml "access plus 1 hour"

  # Default
  ExpiresDefault "access plus 1 week"
</IfModule>

# ─── Additional Cache Headers ───
<IfModule mod_headers.c>
  # Immutable for fingerprinted assets (Astro _astro/ folder)
  <FilesMatch "/_astro/.+\.(css|js)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

  # Fonts
  <FilesMatch "\.(woff2?|ttf|otf|eot)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
</IfModule>
